{
  "$schema": "https://railway.app/railway.schema.json",
  "//": "Maverick on Railway — reference service. Add a Volume mounted at /state and set ANTHROPIC_API_KEY + MAVERICK_DASHBOARD_TOKEN in the service variables. Railway injects $PORT; the start command binds the dashboard to it.",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "deploy/docker/Dockerfile"
  },
  "deploy": {
    "startCommand": "maverick dashboard --host 0.0.0.0 --port ${PORT:-8765}",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "numReplicas": 1
  }
}
