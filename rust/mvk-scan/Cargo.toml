[package]
name = "mvk-scan"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Maverick native safety scanners (pure logic; shared by the Python and WASM bindings)."

[dependencies]
unicode-normalization = "0.1"
